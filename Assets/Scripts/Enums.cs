using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum EnumPlayer
{
    Unassigned,
    P1,
    P2
}

public enum EnumPartShape
{
    Cube,
    Heart,
    Pyramid,
    Torus,
}

public enum EnumPartColor
{
    Blue,
    Green,
    Red,
    Yellow,
}

public enum EnumGameMode
{
    ClearShapes,
    TimedGame,
}
